class User {

  final int id;
  final String userId;
  final String nickname;
  final String profile;  
  final String about;
  //authorInfo 추가예정
  User(this.id, this.userId, this.nickname, this.profile, this.about);
}